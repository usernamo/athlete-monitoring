# Публичный URL API. Используется в debug и release, если в local.properties нет DEMO_API_URL.
# Скопируйте домен из Vercel → Project → Settings → Domains (со слэшем в конце).
# Пример: PRODUCTION_API_URL=https://athlete-monitoring-xxxx.vercel.app/
PRODUCTION_API_URL=
